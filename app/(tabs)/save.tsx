import { Text, View } from "react-native";

const Save = () => {
  return (
    <View className="flex-1 bg-primary">
      <Text className="text-white text-6xl">Save</Text>
    </View>
  );
};

export default Save;
