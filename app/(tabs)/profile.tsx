import { Text, View } from "react-native";

const Profile = () => {
  return (
    <View className="flex-1 bg-primary">
      <Text className="text-white text-6xl">Profile</Text>
    </View>
  );
};

export default Profile;
